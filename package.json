{
  "name": "vibi",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "viBI – AI-assisted PowerBI report designer (web UI + local Windows helper).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "helper:dev": "npm --prefix helper run dev",
    "helper:build": "npm --prefix helper run build",
    "helper:start": "npm --prefix helper start"
  },
  "dependencies": {
    "framer-motion": "^11.11.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
